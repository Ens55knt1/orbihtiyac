<Window x:Class="OrbisisIhtiyac.Wpf.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ORBİSİS İhtiyaç - Giriş"
        Height="520" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Grid Margin="0" Background="Transparent">
        <!-- Dış telefon çerçevesi -->
        <Border Background="{StaticResource BgBrush}"
                CornerRadius="32"
                Margin="8"
                MouseLeftButtonDown="OnDragWindow">
            <!-- İç kart -->
            <Border Background="{StaticResource CardBrush}" CornerRadius="26" Padding="24" BorderBrush="#3B4A65" BorderThickness="1">
                <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Üst başlık logo tarzı -->
                <TextBlock Text="ORBİSİS İHTİYAÇ"
                           FontSize="24"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Foreground="White" />
                <TextBlock Grid.Row="1" Text="Giriş / Kayıt" Foreground="{StaticResource MutedBrush}" HorizontalAlignment="Center" Margin="0,6,0,12"/>

                <StackPanel Grid.Row="2">
                    <!-- Giriş / Kayıt pill sekmeleri -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,12">
                        <ToggleButton x:Name="TabLogin" Content="Giriş" Style="{StaticResource PillToggle}" Margin="0,0,8,0" Click="OnShowLoginClick"/>
                        <ToggleButton x:Name="TabRegister" Content="Kayıt" Style="{StaticResource PillToggle}" Click="OnShowRegisterClick"/>
                    </StackPanel>

                    <!-- Giriş paneli -->
                    <StackPanel x:Name="LoginPanel">
                        <TextBlock x:Name="LoginErrorText" Foreground="#F87171" TextWrapping="Wrap" Margin="0,0,0,8"/>

                        <TextBlock Text="İsim (admin için: admin)" Foreground="{StaticResource MutedBrush}" />
                        <TextBox x:Name="LoginIsim" Height="34" Margin="0,6,0,10"/>

                        <TextBlock Text="Soyisim (admin için boş bırakın)" Foreground="{StaticResource MutedBrush}" />
                        <TextBox x:Name="LoginSoyisim" Height="34" Margin="0,6,0,10"/>

                        <TextBlock Text="Parola (admin: 123456)" Foreground="{StaticResource MutedBrush}" />
                        <PasswordBox x:Name="LoginPassword" Height="34" Margin="0,6,0,12"/>

                        <Button Content="Giriş yap" Style="{StaticResource PrimaryButton}" Click="OnLoginClick" Margin="0,4,0,0"/>
                    </StackPanel>

                    <!-- Kayıt paneli -->
                    <StackPanel x:Name="RegisterPanel" Visibility="Collapsed">
                        <TextBlock x:Name="RegisterErrorText" Foreground="#F87171" TextWrapping="Wrap" Margin="0,0,0,8"/>

                        <TextBlock Text="İsim *" Foreground="{StaticResource MutedBrush}" />
                        <TextBox x:Name="RegIsim" Height="34" Margin="0,6,0,10"/>

                        <TextBlock Text="Soyisim" Foreground="{StaticResource MutedBrush}" />
                        <TextBox x:Name="RegSoyisim" Height="34" Margin="0,6,0,10"/>

                        <TextBlock Text="Parola (en az 4 karakter) *" Foreground="{StaticResource MutedBrush}" />
                        <PasswordBox x:Name="RegPassword" Height="34" Margin="0,6,0,12"/>

                        <Button Content="Kayıt ol" Style="{StaticResource PrimaryButton}" Click="OnRegisterClick" Margin="0,4,0,0"/>
                    </StackPanel>
                </StackPanel>
                </Grid>
            </Border>
        </Border>
    </Grid>
</Window>

