<Window x:Class="OrbisisIhtiyac.Wpf.Views.PasswordChangeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Şifre"
        Height="260" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Border Background="{StaticResource CardBrush}" CornerRadius="18" Padding="16" BorderBrush="#3B4A65" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Yeni parola belirleyin (isteğe bağlı)" FontWeight="Bold" FontSize="16" Margin="0,0,0,6"/>
                <TextBlock x:Name="Err" Foreground="#F87171" Margin="0,0,0,10"/>

                <TextBlock Text="Yeni parola (en az 4 karakter)" Foreground="{StaticResource MutedBrush}"/>
                <PasswordBox x:Name="NewPw" Height="34" Margin="0,6,0,12"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Atla" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Click="OnSkip"/>
                    <Button Content="Kaydet" Style="{StaticResource PrimaryButton}" Click="OnSave"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>

